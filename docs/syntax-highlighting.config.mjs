export const SYNTAX_THEMES = {
	light: "github-light",
	dark: "github-dark",
};

// Fallback key used by Astro's Code component before theme-aware CSS applies.
export const SYNTAX_DEFAULT_COLOR = "light";

// Shared language for declaration snippets in code-reference pages.
export const DECLARATION_LANGUAGE = "cpp";
